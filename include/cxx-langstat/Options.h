#ifndef OPTIONS_H
#define OPTIONS_H

//-----------------------------------------------------------------------------

enum Stage {
    none, emit_features=1, emit_statistics=2
};
// Not used currently
enum AnalysisType {
    ala, cca, cea, cla, fpa, lda, lka, tia, tpa, ua, ula, vta
};

//-----------------------------------------------------------------------------

#endif // OPTIONS_H

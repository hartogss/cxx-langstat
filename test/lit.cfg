# lit can also be installed via pip3 install lit

import lit.util
import lit.formats

# if I understand correctly, this file configures lit globally

config.name = 'cxx-langstat'
config.test_format = lit.formats.ShTest(True)
config.suffixes = ['.cpp', '.cxx', '.cc']
